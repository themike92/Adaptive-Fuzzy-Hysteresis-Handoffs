# Adaptive-Fuzzy-Hysteresis-Handoffs
Exploring dynamic and signal aware handoffs in a wireless mobile cell network. We will do this by running a simulation.

## Environment Activation:
venv\Scripts\activate

## Git commands:
git status

git add.

git commit -m ""

git push origin branch-name
